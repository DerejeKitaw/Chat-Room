extend layout

block content
  h1 Edit Chat room
  form(method="post")
    fieldset.form-group
    lable(for="name") Name:
      input.form-control(name="name", type="text",placeholder="Enter a name",value=room.name)
      small.text-muted Give your chat room a meaningfull name for people to refer to it.
    button.btn.btn-primary(type="submit") Save chat room
    a.btn.btn-default(href="/admin/rooms") Cancel